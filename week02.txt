week02
- linux command: ls(현재폴더안에있는거보기), ls -al(숨긴거까지보기), pwd(현재위치보기), cd .git, cd . , echo "blahblah" >> README.md
- git command:
git init
git status
git add week01.txt
git config --global user.email "you@example.com"
git config --global user.name "your name"
git commit -m "v0"
git log
git chceckout hashcode
git checkout -
git branch -M main
git remote add origin https://github.com/sarangju/test.git
git remote -v
git push -u origin main
git clone https://github.com/sarangju/test.git (.)
*.을 찍으면 현재 폴더로 가져오는 것


inhatckimtaeganc